using NexaSoft.Club.Domain.Masters.FeeConfigurations;

namespace NexaSoft.Club.Domain.Specifications;

// Specification para configuraciones mensuales activas por tipo de miembro
/*public class ActiveMonthlyFeesByMemberTypeSpec : BaseSpecification<FeeConfiguration>
{
    public ActiveMonthlyFeesByMemberTypeSpec(long memberTypeId) : base()
    {
        AddCriteria(x => x.MemberTypeId == memberTypeId &&
                       x.Periodicity == "Monthly" &&
                       x.IsActive);
    }
}

// Specification para configuraciones NO mensuales activas por tipo de miembro
public class ActiveNonMonthlyFeesByMemberTypeSpec : BaseSpecification<FeeConfiguration>
{
    public ActiveNonMonthlyFeesByMemberTypeSpec(long memberTypeId) : base()
    {
        AddCriteria(x => x.MemberTypeId == memberTypeId &&
                       x.Periodicity != "Monthly" &&
                       x.IsActive);
    }
}

// Specification para todas las configuraciones activas por tipo de miembro
public class ActiveFeesByMemberTypeSpec : BaseSpecification<FeeConfiguration>
{
    public ActiveFeesByMemberTypeSpec(long memberTypeId) : base()
    {
        AddCriteria(x => x.MemberTypeId == memberTypeId && x.IsActive);
    }
}

// Specification para configuraciones anuales activas
public class ActiveYearlyFeesByMemberTypeSpec : BaseSpecification<FeeConfiguration>
{
    public ActiveYearlyFeesByMemberTypeSpec(long memberTypeId) : base()
    {
        AddCriteria(x => x.MemberTypeId == memberTypeId &&
                       x.Periodicity == "Yearly" &&
                       x.IsActive);
    }
}

// Specification para configuraciones one-time activas
public class ActiveOneTimeFeesByMemberTypeSpec : BaseSpecification<FeeConfiguration>
{
    public ActiveOneTimeFeesByMemberTypeSpec(long memberTypeId) : base()
    {
        AddCriteria(x => x.MemberTypeId == memberTypeId &&
                       x.Periodicity == "OneTime" &&
                       x.IsActive);
    }
}*/
